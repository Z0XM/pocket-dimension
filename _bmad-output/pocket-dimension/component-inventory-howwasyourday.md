# Component Inventory — howwasyourday

Vendor `src/lib/components/ui/**` omitted.

## Layout

- `HelloUser.svelte` — greeting, sign-out
- `PwaInstallButton.svelte`

## Feature

- `(protected)/+page.svelte` — year calendar / graph (inline)
- `NotificationPrompt.svelte` — push + reminder time
- `DrawingCanvas.svelte`
- `ColorPicker.svelte`

## Form

- `(protected)/day/[dayInt]/+page.svelte` — day journal (`enhance`)
