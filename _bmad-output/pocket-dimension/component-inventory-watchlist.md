# Component Inventory — watchlist

Vendor kit (`src/lib/components/ui/**`, bits-ui) omitted.

## Layout

- `overlay.svelte` — nav, views, auth, mobile menu
- `background.svelte` — glass background
- `pwa-install-button.svelte`, `theme-button.svelte`, `logout-button.svelte`

## Feature

- `(public)/data-table.svelte` — TanStack Table
- `(public)/data-table-helpers/*` — edit, bulk, filters, infinite scroll, column settings
- `leaderboard/leaderboard-content.svelte`
- `dashboard/` — stat-card, donut, histogram, bar charts

## Form / dialog

- `add-item-dialog`, `bulk-edit-panel`, `unsaved-changes-dialog`, `delete-confirmation-dialog`, `mobile-filter-dialog`, `row-details-dialog`
