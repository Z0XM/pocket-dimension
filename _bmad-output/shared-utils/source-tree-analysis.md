# Source Tree — `@pocket-dimension/utils`

```
shared/utils/
├── package.json
├── tsconfig.json          # extends ../../tsconfig.base.json
├── src/
│   └── index.ts           # only source module — all exports
└── dist/                  # bun build output (gitignored)
```
