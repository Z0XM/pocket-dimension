# Story 4.2: Connect to LiveKit and render participant grid

**Epic:** 4 — Pre-call lobby and in-call video UI  
**Status:** done

## Acceptance criteria

- [x] Client connects with minted token on join
- [x] VideoGrid layouts for 1–6 participants
- [x] Name overlay on each tile
- [x] Avatar/initials when camera off

## Implementation

- `room-client.ts`, `VideoGrid.svelte`, `ParticipantTile.svelte`, `CallExperience.svelte`
