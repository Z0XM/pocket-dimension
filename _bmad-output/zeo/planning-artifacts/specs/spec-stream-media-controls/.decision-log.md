# Decision log — spec-stream-media-controls

## 2026-07-25 — Create (express distill)

**Mode:** Headless/express from user brain-dump of five minor in-call features.  
**Path chosen:** `bmad-spec` (lean brownfield contract) rather than full PRD→UX→Architecture ceremony used for Game Mode — scope is incremental call UX, not a new product phase.

### Routing decisions

| Claim | Landed in |
|-------|-----------|
| Single share button; tile video/audio share toggles; default both on | SPEC CAP-1 + ux.md + stories 16.1 |
| Per-tile volume slider beside level indicator | SPEC CAP-2 + ux.md + stories 16.2 |
| Global A/V quality in Settings | SPEC CAP-3 + ux.md + stories 16.3 |
| Per-tile ping + V/A quality numbers; settings master toggle | SPEC CAP-4 + ux.md + stories 16.4 |
| Nonlinear level meter (75% / 25% split) | SPEC CAP-5 + ux.md + stories 16.5 |
| Existing files / LiveKit APIs / storage keys | brownfield.md |
| Mic/camera stay on control bar | SPEC Non-goals |

### Assumptions locked without user confirmation

- CAP-1 = screen/tab share consolidation (not mic/cam relocation).
- Quality = local publish + subscribe presets, High ≤720p.
- Stats default on; volumes session-scoped.
- Knee constant starts at `0.3` pending tuning.

### Open questions left for human

See SPEC.md Open Questions (both-off auto-stop; audio-only entry removal; mid-call republish; exact stats fields; volume persistence).

### Self-validate

**Pass 1 — Coherence:** CAP-1…5 each have intent+success; constraints rule out redesign/recording/host policy; non-goals present; success signal demonstrable; IDs stable. Lean enough for downstream create-story.

**Pass 2 — Preservation:** All five user feature asks preserved. Wrapper-only content dropped: none material. Implied “stream tile” = screen-share tile for CAP-1 and any tile with audio chrome for CAP-2/4/5 — recorded as assumptions.

**Verdict:** Spec package ready for review. Next BMAD step after open questions resolved (or accepted as assumptions): `bmad-sprint-planning` / `bmad-create-story` for Epic 16, then implement — user asked to plan first.
