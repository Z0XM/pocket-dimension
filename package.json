{
  "name": "pocket-dimension",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "build:shared:auth": "turbo run build --filter=@pocket-dimension/auth",
    "build:shared:db": "turbo run build --filter=@pocket-dimension/db",
    "build:shared:utils": "turbo run build --filter=@pocket-dimension/utils",
    "build:app:auth": "turbo run build --filter=@pocket-dimension/auth-service",
    "build:app:watchlist": "turbo run build --filter=@pocket-dimension/watchlist-svelte",
    "dev:app:auth": "turbo run dev --filter=@pocket-dimension/auth-service",
    "dev:app:watchlist": "turbo run dev --filter=@pocket-dimension/watchlist-svelte",
    "db:generate": "turbo run db:generate --filter=@pocket-dimension/db --",
    "db:migrate": "turbo run db:migrate --filter=@pocket-dimension/db",
    "db:studio": "turbo run db:studio --filter=@pocket-dimension/db",
    "lint": "turbo run lint",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "check": "biome check .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "turbo run build --filter='[HEAD^1]' && changeset publish",
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@changesets/cli": "latest",
    "@types/bun": "^1.3.5",
    "@vitest/coverage-v8": "latest",
    "husky": "latest",
    "lint-staged": "latest",
    "turbo": "latest",
    "typescript": "latest",
    "vitest": "latest"
  },
  "packageManager": "bun@1.1.38",
  "workspaces": [
    "apps/**",
    "shared/**"
  ],
  "dependencies": {
    "@elysiajs/cors": "^1.4.0",
    "better-auth": "^1.4.9",
    "elysia": "^1.4.19"
  }
}
