# Sample Reference Assets

This folder preserves the UI reference assets used by the `/sample` route so the look and feel can be revisited while building the real app.

## Files

- `app.sample.css` - snapshot copy of the global styling base used when the sample was created.
- `theme-presets.json` - exported base theme presets used by the sample theme selector.

The live sample page is available at `/sample`.
