{
  "name": "@pocket-dimension/backend-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "bun run src/index.ts",
    "start": "bun run dist/index.js",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "@types/node": "latest",
    "typescript": "^5.7.2"
  }
}
