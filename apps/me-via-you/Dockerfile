# Moved to monorepo root: ../../Dockerfile.me-via-you
#
# Dokploy defaults Docker context to the Dockerfile's directory. A Dockerfile
# inside apps/me-via-you cannot see scripts/, shared/, or the root package.json.
#
# Use these Dokploy settings instead:
#   Build path: /
#   Dockerfile path: Dockerfile.me-via-you
#   Docker context path: (leave empty)
